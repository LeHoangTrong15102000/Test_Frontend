import http from "@/lib/http"


const prefix = 'mbkl49vjwpgtw92/records'
const orderItemApiRequest = {
  // list: () => http.get<any>(),
  // add: (body: any) => http.post<any>(),
  // getOrderItem: (recordId: number) => http.get<any>(),
  // updateOrderItem: (recordId: number, body: any) => http.patch<any>(),
  // deleteOrderItem: (recordId: number) => http.delete<any>()
}


export default orderItemApiRequest